Пока ВыборкаДанныхЗапроса.Следующий() Цикл
	НаборЗаписей = РегистрыСведений.АктивацияПодарочныхСертификатов.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.РегистраторУстановить(ВыборкаДанныхЗапроса.Ссылка);
	наборЗаписей.Прочитать();
	Для Каждого Серт из НаборЗаписей Цикл
		Если Серт.ПодарочныйСертификат = Объект.Сертификат Тогда
			НаборЗаписей.Удалить(Серт);
			НаборЗаписей.Записать(Истина);
		КонецЕсли;
	КонецЦикла;
КонецЦикла;
